# В dev при пустом значении Vite проксирует /api → FastAPI (см. vite.config.ts).
# Раскомментируйте только если хотите ходить на API напрямую (нужен CORS на бэкенде):
# VITE_API_BASE=http://127.0.0.1:8000/api/v1
