# Скопируйте в .env в каталоге simple/ и при необходимости заполните.
# Реальный .env в репозиторий не коммитить.

OPENAI_API_KEY=
STORAGE_PATH=./data/files

# Статическая заглушка (этап 1.5). Позже: путь к сборке фронта (например ../frontend/dist).
# WEB_STATIC_PATH=./web
